import type { Metadata } from 'next'

export const metadata: Metadata = { title: 'Listes de choix' }

export default function Layout({ children }: { children: React.ReactNode }) {
  return children
}
